# Positron Framework Requirements

# Core dependency - WebView for modern browser support
# Uses WebView2 (Windows), WebKit (macOS), GTK WebKit (Linux)
pywebview>=6.0

# Development dependencies (optional)
# Uncomment if you want to contribute to Positron development
# pytest>=7.0.0
# black>=23.0.0
# flake8>=6.0.0
